[project]
name = "usefulenv"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "typing-extensions",
    "python-dotenv",
    "openenv"
]

[project.scripts]
server = "server.app:main"
